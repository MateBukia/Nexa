# Server-side target used by the Next.js same-origin /api proxy.
BACKEND_API_URL=http://localhost:4000/api

# Optional: bypass the proxy with a browser-visible API URL.
# NEXT_PUBLIC_API_URL=http://localhost:4000/api
